# Core runtime
fastapi
uvicorn
simpleeval
pyyaml
pandas
python-multipart

# Document ingestion & rendering
# docling is PINNED (TICKETS.md A6): its layout model and bbox coordinate conventions
# feed the provenance chain, so an unreviewed upgrade must not change what ingestion
# extracts. Bump deliberately, then re-ingest and eyeball citations.
docling==2.113.0   # heavy (pulls torch + models); ingestion has an offline fallback
pypdfium2
pillow
reportlab

# Retrieval / search (large-doc + policy Q&A)
sentence-transformers   # local embeddings for hybrid retrieval; BM25 fallback if absent
opensearch-py           # production hybrid backend adapter (needs a running cluster)

# LLM (optional at run time; graceful fallback when ANTHROPIC_API_KEY is unset)
anthropic

# Tests
pytest
